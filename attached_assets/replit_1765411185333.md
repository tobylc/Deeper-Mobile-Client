# Deeper - Connection & Conversation Platform

## Overview
Deeper is a full-stack web application designed to foster meaningful connections and conversations. It enables users to create private conversation spaces based on relationship types (e.g., parent-child, romantic partners) and provides curated question suggestions to deepen dialogue. The platform aims to transform superficial interactions into thoughtful, enduring exchanges, supporting long-term relationship growth.

## User Preferences
Preferred communication style: Simple, everyday language.
Change authorization policy: NEVER make changes to any elements, pages, or functionality without explicit user authorization in advance. Always ask for permission before modifying existing features or design.

## Recent Changes (December 10, 2025)
- **MOBILE API PREPARATION (Phase 1)**: Added JWT-based authentication endpoints for iOS mobile app:
  - `POST /api/mobile/auth/login` - Login with email/password, returns JWT token
  - `POST /api/mobile/auth/signup` - Create account, returns JWT token
  - `GET /api/mobile/auth/user` - Get current user from JWT token
  - `POST /api/mobile/auth/refresh` - Refresh JWT token (only valid tokens can be refreshed)
  - `POST /api/mobile/push-token` - Register Expo push notification token
  - Added `expoPushToken` field to users table for mobile push notifications
  - Added `isMobileAuthenticated` middleware for protecting mobile routes
  - Security: JWT_SECRET required in production, tokens expire in 7 days
- **ALTERNATIVE AUTHENTICATION**: Implemented email/password authentication with bcrypt hashing to support immediate production launch without waiting for Google OAuth verification (4-6 weeks). Users can now:
  - Sign up with email and password immediately
  - Log in with email and password
  - Optionally link Google OAuth when verification is complete
- **GOOGLE OAUTH PRODUCTION PATH**: Production Google OAuth requires adding callback URL to Google Cloud Console:
  - Add to **Authorized JavaScript origins**: `https://joindeeper.com`
  - Add to **Authorized redirect URIs**: `https://joindeeper.com/api/auth/google/callback`
  - This is still pending Google's OAuth application review (4-6 weeks typical)

## Previous Changes (August 27, 2025)
- **CRITICAL PRODUCTION FIX**: Fixed mobile scrolling issue on all popup components to prevent users from being unable to interact with buttons on iPhone browsers. Applied `max-h-[90vh] overflow-y-auto` and proper flex layouts to:
  - QuestionChoicePopup (new core logic feature)
  - ThoughtfulResponsePopup (existing)
  - NotificationPreferencePopup (existing)
  - TrialExpirationPopup (existing)
  - ExchangeRequiredPopup (existing)
  - WaitingTurnPopup (existing)
  - RespondFirstPopup (existing)
- **CORE LOGIC IMPLEMENTATION**: Successfully implemented question choice detection that intercepts user-typed questions ending with "?" and presents choice between new thread or response.
- **DUAL MESSAGE FUNCTIONALITY**: When user chooses "Start New Question Thread", the same text is now added to both the current conversation thread AND used to create the new thread, providing continuity across conversations.

## System Architecture

### Frontend
- **Framework**: React 18 with TypeScript
- **Routing**: Wouter
- **State Management**: React Context (authentication), TanStack Query (server state)
- **UI Framework**: Radix UI components with shadcn/ui styling
- **Styling**: Tailwind CSS with CSS variables
- **Build Tool**: Vite

### Backend
- **Runtime**: Node.js with Express.js
- **Language**: TypeScript (ES modules)
- **API Pattern**: RESTful API
- **Production Build**: esbuild

### Data Storage
- **Database**: PostgreSQL (Neon serverless)
- **ORM**: Drizzle
- **Schema Management**: Drizzle Kit

### Core Features
- **Authentication**: 
  - Web: Google OAuth + email/password with session cookies, Passport.js
  - Mobile: JWT token-based authentication (7-day expiry)
  - `isMobileAuthenticated` middleware for mobile API routes
- **Connection Management**: Invitation system via email, relationship categorization, status tracking, personal messages.
- **Conversation System**: Private, turn-based conversation spaces with message categorization (questions/responses) and real-time synchronization via WebSockets. Enforces strict turn-based system, question-response exchanges, thoughtful response timers (10 mins for responses), and prohibits timers for questions.
- **Question Choice Detection**: CORE LOGIC that detects when users type questions ending with "?" in the middle column text box. Shows popup asking if they want to create a new question thread or send as response to current thread. If user chooses "new question", text is added to BOTH the current conversation thread AND used to create a new thread. If "response", sent as normal response with all existing logic (including timers).
- **Question Suggestion Engine**: Curated and AI-generated questions categorized by relationship type, populating text input for editing.
- **Journal Entry Interface**: Unique UI transforms conversations into a journal aesthetic with paper textures, ruled lines, and handwriting fonts.
- **Voice Messaging**: Voice recording with OpenAI Whisper AI transcription, integrated into conversation flow.
- **Subscription Model**: Invitee users have "free forever" access; inviter users require trial/paid subscriptions with connection limits.
- **Enhanced Email Campaign System**: Aggressive nudging system with reminders every 48-72 hours for up to 35 days. Includes pending invitation reminders, turn reminders, and inviter nudges with escalating urgency. Features opt-out functionality respecting user notification preferences and unsubscribe links.
- **Thread Management**: Multi-conversation threading system allowing new question threads, with automatic title generation and paper stacking for long conversations.
- **Thoughtful Response Enforcement**: Implements a 10-minute minimum response time for thoughtful engagement, with visual popups.

### Communication Infrastructure Migration (SendGrid/Twilio → AWS)

**MIGRATION STATUS**: Complete - Full AWS integration implemented with zero-downtime migration capability.

**SMS Migration (Twilio → Amazon SNS)**:
- Added `AmazonSNSSMSService` class using AWS SDK v3 (`@aws-sdk/client-sns`)
- Enhanced `createSMSService()` factory with multi-provider support
- Implemented `FallbackSMSService` with automatic fallback to internal database
- Maintained all existing SMS templates and formatting
- Preserved production safety with comprehensive error handling

**Production Migration Status**: 
- **EMAIL**: SendGrid active (temporarily reverted while AWS SES verification pending)
- **SMS**: Twilio active (temporarily reverted while AWS SNS setup pending)

**Architecture Changes**:
- Added `AmazonSESEmailService` class using AWS SDK v3 (`@aws-sdk/client-sesv2`)
- Added `AmazonSNSSMSService` class using AWS SDK v3 (`@aws-sdk/client-sns`)
- Enhanced `createEmailService()` and `createSMSService()` factories with multi-provider support
- Implemented `FallbackSMSService` and `FallbackEmailService` for automatic failover
- Updated email campaigns to send through actual email service instead of just database storage
- Maintained all existing email and SMS templates and formatting
- Preserved fallback system to internal database for production safety

**Production Migration Instructions**:
1. **Current Status**: AWS services active as default (SES for email, SNS for SMS)
2. **Rollback Email**: Set `EMAIL_PROVIDER=sendgrid` to revert if needed  
3. **Rollback SMS**: Set `SMS_PROVIDER=twilio` to revert if needed
4. **Force AWS**: Set `EMAIL_PROVIDER=ses` and `SMS_PROVIDER=sns` (explicit)

**Required Environment Variables for AWS**:
- `EMAIL_PROVIDER` (optional: 'ses' or 'sendgrid', defaults to SES)
- `SMS_PROVIDER` (optional: 'sns' or 'twilio', defaults to SNS)
- `AWS_ACCESS_KEY_ID` (existing)
- `AWS_SECRET_ACCESS_KEY` (existing) 
- `AWS_REGION` (existing, defaults to 'us-east-1')

**Communication Types Migrated**:
- **Email**: Connection invitations, acceptance/decline notifications, turn notifications, all email campaigns
- **SMS**: Connection invitations, acceptance/decline notifications, turn notifications, verification codes

**Production Safety Features**:
- Automatic fallback to internal database if external service fails
- Comprehensive logging for monitoring and debugging
- Maintains existing email templates and user experience
- Zero-downtime migration capability
- Production environment detection and safety checks

### UI/UX Design
- **Color Scheme**: Dark sophisticated ocean blue and amber palette with multi-layer radial gradient backgrounds.
- **Aesthetics**: Curved aesthetic (1.5rem radius), glassmorphism navigation, gradient-based buttons.
- **Typography**: Modern Inter font, with Kalam for journal entries.
- **Responsiveness**: Mobile-first approach with consistent padding and optimized layouts.

## External Dependencies

### Production Dependencies
- **@neondatabase/serverless**: PostgreSQL driver
- **drizzle-orm**: ORM
- **@tanstack/react-query**: Server state management
- **@radix-ui/***: UI component primitives
- **wouter**: React router
- **zod**: Schema validation
- **tailwindcss**: CSS framework
- **Amazon SNS**: SMS notifications (migrated from Twilio)
- **Twilio**: SMS notifications (legacy, being phased out)
- **Amazon SES**: Email delivery (migrated from SendGrid)
- **SendGrid**: Email delivery (legacy, being phased out)
- **OpenAI Whisper**: AI transcription
- **Stripe**: Subscription billing and payment processing

### Development Dependencies
- **tsx**: TypeScript execution
- **esbuild**: JavaScript bundler
- **vite**: Frontend build tool
- **drizzle-kit**: Database schema management